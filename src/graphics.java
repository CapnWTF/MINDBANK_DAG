public class graphics {
    /*TODO if possible, generate a GUI to represent the DAG
        the GUI should be basic and use simple shapes to represent nodes, have the id of the node centered,
        use colors for the nodes and edges, and be able to change the colors for the nodes to represent
        pathing.
     */
    
    // public void paint (Graphics g) {
    //     Graphics2D g2 = (Graphics2D g);
    // }
}
